import Icon from "@mdi/react";
import { mdiCalendarCheck, mdiAccountPlus, mdiCheckCircle } from "@mdi/js";

import ComponentAriaChart from "@/components/BusinessAdmin/BusinessDashboard/AppointmentLineChart";
import ComponentServices from "@/components/BusinessAdmin/BusinessDashboard/ServiceDonutChart";

const BusinessAdminContainer = () => {
  return (
    <div className="flex">
      <aside className="md);
};

const stats = [
  {
    label,
    value,
    icon,
    color,
  },
  {
    label,
    value,
    icon,
    color,
  },
  {
    label,
    value,
    icon,
    color,
  },
  {
    label,
    value,
    icon,
    color,
  },
];

const Dashboard = () => {
  return (
    <div className="p-2 space-y-6 ">
      <div className="grid grid-cols-1 md, i) => (
          <div
            key={i}
            className="flex items-center gap-4 p-5 bg-white rounded-2xl shadow-md transition hover))}
      </div>

      <div className="grid grid-cols-1 md);
};

const Copyright = () => (
  <div className="ml-5 mb-5">
    <span style={{ fontSize, color, fontWeight);

export default BusinessAdminContainer;
