services:
  - type: web
    name: your-app-name
    env: static
    buildCommand: npm run build
    staticPublishPath: ./dist
    headers:
      - path: /*
        headers:
          - key: X-Frame-Options
            value: DENY
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
